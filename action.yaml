name: gsheet.action Update-data
on:
  schedule: 
  - cron: '0 1 * * *'
jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - id: 'get_gsheet_data'
        uses: jroehl/gsheet.action@v1.0.0 
        with:
          spreadsheetId: <spreadsheetId>
          commands: | 
            [
              { "command": "updateData", "args": { "data": [["A1", "A2", "A3"]] }},
              { "command": "getData", "args": { "range": "'<worksheetTitle>'!A2:B3" } }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}
      - name: dump results
        env:
          RESULTS: ${{ steps.get_gsheet_data.outputs.results }}
        run: touch "$RESULTS" | something.csv # need to change this
      - id: 'clean_data'
      uses: actions/checkout@v2
      uses: actions/setup-python@v2
      with:
        python-version: '3.x' 
      name: run python script 
      run: python my_script.py
